import {RichtextFlattenPipe} from './richtext-flatten.pipe';

describe('RichtextFlattenPipe', () => {
  it('create an instance', () => {
    const pipe = new RichtextFlattenPipe();
    expect(pipe).toBeTruthy();
  });
});
