version: '3'

services: 
  # GrpcWeb proxy, running at port :8080
  # Uses host network
  # Assumes the Grpc backend is running at localhost:5001
  envoy:
    build:
      context: ./server/envoy
      dockerfile: Dockerfile
    image: ztl8702/yngdieng-api-frontend:dev
    network_mode: "host"
